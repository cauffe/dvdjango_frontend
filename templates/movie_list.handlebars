{{#each movies}}
    <div class="row">
      <div class="col-md-12 movie" id="{{ pk }}">
        <h2 id="{{ pk }}">Title:{{ title }}</h2>
        <p><a class="btn btn-default" href="#" role="button">View details &raquo;</a></p>
      </div>
    </div>
{{/each}}